import React from 'react';
import ProgressBar from 'ee-kitchen-sink/ProgressBar';

/** Progress bar with 70 percent, width 150. */
export default function Example70Percent() {
    return <ProgressBar percent={70} width={150} />
}